const fs = require('fs')

exports.import = function(req, res) {
    // Get data

    // Loop over data
        // Insert single products
        // Insert products with sub products

    // Check if successful

    // productName,productDescription,productShortDescription,productCategoryId,productImage,productColor,storageLocation,availableFrom,availableTo,measurement,measurementUnitId,sku,sellAs,customSaleUnit,qty,price,salePrice,unitPrice,taxClassId,businessId,published,state,createdAt,updatedAt		
        
}